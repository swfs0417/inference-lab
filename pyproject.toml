[project]
name = "inference-lab"
version = "0.1.0"
description = "OpenAI-compatible streaming API benchmark"
requires-python = ">=3.10"
dependencies = [
    "openpyxl==3.1.5",
    "pymupdf4llm==1.28.0",
]
